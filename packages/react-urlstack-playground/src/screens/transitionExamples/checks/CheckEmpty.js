import React from 'react'
import { View, Text } from 'react-native'

function CheckEmpty(props) {
	return (
		<View>
			<View>
				<Text>This screen is empty. The only one that has contents is the check tab.</Text>
			</View>
		</View>
	)
}

export default CheckEmpty;